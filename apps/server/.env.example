# ── 本地开发（Node 跑在宿主机，连 compose 暴露出来的端口）──
PORT=3100
MONGODB_URL=mongodb://localhost:27017/scene-prod
REDIS_URL=redis://localhost:6380

# ── 生产（仅供 docker-compose.prod.yml 注入，server 容器内走服务名访问）──
# 把下面的值放进根目录的 .env（不要提交），compose 会读取：
#   MONGO_USER=app
#   MONGO_PASSWORD=<强密码>
#   REDIS_PASSWORD=<强密码>
